# คู่มือการ Deploy เว็บไซต์ขึ้น GitHub Pages

คู่มือนี้จะอธิบายขั้นตอนการอัปเดตโค้ดและนำขึ้นระบบ (Deploy) สำหรับโครงการ **KidsApps (larn-english-vocab)** ค่ะ

---

## 🛠️ ขั้นตอนการบันทึกโค้ดและ Deploy

การทำงานของโปรเจกต์นี้จะแบ่งเป็น 2 ส่วนหลัก ๆ คือการบันทึกโค้ดต้นฉบับ (Source Code) ไปที่สาขา `main` และการนำส่งไฟล์เว็บที่ประกอบเสร็จแล้ว (Build Artifacts) ไปที่สาขา `gh-pages` เพื่อแสดงผลจริงค่ะ

### ขั้นตอนที่ 1: บันทึกโค้ดต้นฉบับไปยังสาขา `main`
ก่อนที่จะทำการ Deploy ทุกครั้ง ควรทำการ Commit และ Push โค้ดต้นฉบับขึ้นไปยัง GitHub ก่อนค่ะ เพื่อความปลอดภัยของข้อมูลต้นฉบับ

รันคำสั่งเหล่านี้ใน Terminal:
```bash
git add -A
git commit -m "บันทึกข้อความอธิบายการแก้ไขของคุณ"
git push origin main
```

### ขั้นตอนที่ 2: นำส่งเว็บไซต์ขึ้นระบบ (Deploy to GitHub Pages)
ทำการรันสคริปต์สำหรับสร้างโค้ดหน้าเว็บ และนำขึ้นสาขา `gh-pages` โดยตรง:

#### 💡 สำหรับเครื่องทั่วไป (หรือ macOS / Linux / Git Bash):
```bash
npm run deploy
```

#### ⚠️ สำหรับ Windows PowerShell (หากเจอปัญหาสิทธิ์การรันสคริปต์):
ถ้าคุณใช้ Windows PowerShell แล้วพบข้อผิดพลาดเกี่ยวกับ Security Policy (เช่น *npm.ps1 cannot be loaded because running scripts is disabled*):
คุณสามารถรันคำสั่งผ่าน Command Prompt (CMD) แทนได้โดยใช้รูปแบบนี้ค่ะ:
```bash
cmd /c "npm run deploy"
```

---

## 🔍 วิธีตรวจสอบผลลัพธ์
เมื่อรันขั้นตอนการ Deploy สำเร็จแล้ว จะปรากฏข้อความตัวสุดท้ายว่า `Published` บน Terminal ค่ะ

หลังจากนั้น:
1. ให้เข้าไปที่ GitHub Repository ของคุณในเว็บบราวเซอร์
2. ไปที่แท็บ **Actions**
3. คุณจะเห็น Workflow ที่ชื่อ **pages build and deployment** กำลังทำงานอยู่ (กำลัง Build/Deploy ในระบบของ GitHub)
4. รอประมาณ 1-2 นาที เมื่อหน้า Actions ขึ้นไอคอนถูกสีเขียว (✅) แปลว่าข้อมูลล่าสุดได้รับการอัปเดตบนหน้าเว็บเรียบร้อยแล้วค่ะ!

---
🌸 *จัดทำคู่มือโดย: มะลิ*
